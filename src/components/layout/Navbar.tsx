import Link from "next/link";

export function Navbar() {
    return (
        <nav className="absolute top-0 left-0 w-full p-6 z-50">
            {/* Logo/Text removed as per request */}
        </nav>
    );
}
